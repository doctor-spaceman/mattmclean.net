document.addEventListener('DOMContentLoaded', () => {
  const icons = document.querySelectorAll('.error404 #walkway svg');
  if ( icons.length ) { initWalkway('.error404 #walkway svg', '1000'); }
});